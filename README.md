# ShrinkTexture
This Windows utility recursively downsizes and optionally converts textures inside a folder using ImageMagick. It is designed to normalize oversized asset packs (e.g. 4K textures) into sane, production-ready sizes while keeping repositories clean and manageable.
